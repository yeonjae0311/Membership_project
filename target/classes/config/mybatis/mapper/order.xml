<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="od">

	<!-- p_order 테이블에 주문내역 생성 -->
	<insert id="porder_insert" parameterType="int">
		insert into p_order values(CONCAT(TO_CHAR(sysdate,'YYYYMMDD'),
								   TO_CHAR(SEQ_p_order_IDX.nextval)), #{m_idx))	
	</insert>
	
	<!-- order_detail 테이블에 상세 주문내역 생성 -->
	<insert id="order_detail_insert">
		insert into order_detail values(SEQ_ORDER_DETAIL_IDX.nextval, 
	</insert>

</mapper>


