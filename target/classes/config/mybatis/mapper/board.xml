<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="b">

	<!-- 사고 방지를 위해서 일단은 alias를 이쪽에서는 사용하지 않고 테스트 -->
	
	<!-- 고정된 게시글 조회 -->
	<select id="fixed_board_list" resultType="vo.BoardPMemberViewVO">
		SELECT * FROM board_p_member_join_view
		
	</select>
	
	<select id="unfixed_master_board_list" resultType="vo.BoardPMemberViewVO">
		SELECT * FROM board_p_member_join_view
		
	</select>
	
	<select id="unfixed_fan_board_list" resultType="vo.BoardPMemberViewVO">
		SELECT * FROM board_p_member_join_view
		
	</select>

</mapper>


